import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderColor: "#e2e8f0",
    borderRadius: 15,
    overflow: "hidden",
  },
  contentWrap: {
    margin: 10,
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    gap: 10,
  },
});
